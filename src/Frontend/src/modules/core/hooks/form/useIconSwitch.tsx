import { withTiming, useSharedValue, useAnimatedProps, useAnimatedStyle } from 'react-native-reanimated';

export const useIconSwitch = () => {
    const 


    const animatedStyles = useAnimatedStyle(() => {
        return {
            
        };
    });
}
